{
  "name": "backend_catalogue",
  "version": "1.0.0",
  "description": "backend that deals with request from the frontend. it has access to the mongoDB",
  "author": "Julien Chevalley",
  "license": "MIT",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "lint": "eslint .",
    "test": "cross-env NODE_ENV=test node --test  --test-concurrency=1",
    "test:only": "cross-env NODE_ENV=test node --test --test-only",
    "test:name": "cross-env NODE_ENV=test node --test --test-name-pattern",
    "start:test": "cross-env NODE_ENV=test node --watch index.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.5.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.2.1",
    "globals": "^17.5.0"
  }
}
